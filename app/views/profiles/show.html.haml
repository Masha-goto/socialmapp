.container
	%header.header
		.header_icon
			- if !current_page?(root_path)
				= link_to :back do
					= image_tag 'arrow.svg', class: 'header_back'
			.header_account
				= @profile.account
	.profile_icon
		= image_tag @profile.user.avatar_image